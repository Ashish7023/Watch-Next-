import {IMAGE_URL} from '../constants/constants';

export const Image500 = path => {
  return `${IMAGE_URL}/w500/${path}`;
};
export const Image342 = path => {
  return `${IMAGE_URL}/w342/${path}`;
};
export const Image185 = path => {
  return `${IMAGE_URL}/w185/${path}`;
};
